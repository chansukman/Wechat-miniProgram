<!--pages/work2/work2.wxml-->
<view class="work2-body" wx:for="{{proList}}" wx:key="key">
<view class="work2-title">{{item.title}}</view>
<text class="work2-desc">{{item.desc}}</text>
<view>
<image class="work2-image" src="{{item.image}}"></image>
</view>
</view>
